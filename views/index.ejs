<html>
  <head>
    <title>Home</title>
  </head>

  <body>
    <h1>Homepage</h1>

    <% messages.forEach((message) => { %>
    <p><%= message.user %>: <%= message.text %></p>
    <% }) %>
  </body>
</html>
